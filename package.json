{
  "name": "@kabukki/wasm-nes",
  "version": "1.0.0",
  "main": "dist/index.js",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "build:wasm": "wasm-pack build -t web --debug --scope kabukki --out-name index",
    "build:js": "rollup -c && tsc --emitDeclarationOnly",
    "build": "npm run build:wasm && npm run build:js",
    "lint": "cargo clippy",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^13.0.0",
    "@rollup/plugin-typescript": "^8.2.1",
    "rollup": "^2.52.2",
    "rollup-plugin-copy": "^3.4.0",
    "tslib": "^2.3.0",
    "typescript": "^4.3.5"
  }
}
