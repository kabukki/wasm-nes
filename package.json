{
  "name": "@kabukki/wasm-nes",
  "version": "1.0.0",
  "main": "dist/index.js",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "build:wasm": "wasm-pack build -t web --debug --scope kabukki --out-name index",
    "build:js": "esbuild --bundle --outdir=dist --sourcemap --format=esm index.ts && tsc --emitDeclarationOnly && cp pkg/*.wasm dist",
    "build": "npm run build:wasm && npm run build:js",
    "lint": "cargo clippy",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.12.15",
    "typescript": "^4.3.5"
  }
}
